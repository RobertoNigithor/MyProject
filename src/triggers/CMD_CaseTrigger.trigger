trigger CMD_CaseTrigger on Case (before insert, before update) {

/*CMD_CaseTriggerClass cts = new CMD_CaseTriggerClass();

if(Trigger.isInsert){

    cts.assignmentCase(Trigger.new);
}
else if(Trigger.isUpdate){
   // if(!lstCases.isEmpty()){

    cts.assignmentCase(Trigger.new);
    }

    */
  //}
}